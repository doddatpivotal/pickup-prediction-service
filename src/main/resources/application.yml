spring:
  application:
    name: pickup-prediction-service
management:
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      enabled: true
      show-details: always
  metrics:
    export:
      datadog:
        enabled: false
